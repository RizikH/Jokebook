extends partials/layout.pug

block layout-content
  main.main 
    h1.main-header #{page}
    form#jokeForm(action="/jokebook/new" method="POST")
      label(for="category") Category name:
      input#category(type="text" name="category" required)
      br
      br

      label(for="setup") Setup:
      input#setup(type="text" name="setup" required)
      br
      br

      label(for="delivery") Delivery:
      input#delivery(type="text" name="delivery" required)
      br
      br

      button(type="submit") Submit
